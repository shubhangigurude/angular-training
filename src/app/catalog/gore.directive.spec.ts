import { GoreDirective } from './gore.directive';

describe('GoreDirective', () => {
  it('should create an instance', () => {
    const directive = new GoreDirective();
    expect(directive).toBeTruthy();
  });
});
