import { AppProductHoverDirective } from './app-product-hover.directive';

describe('AppProductHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AppProductHoverDirective();
    expect(directive).toBeTruthy();
  });
});
